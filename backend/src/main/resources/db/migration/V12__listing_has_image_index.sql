-- Отбор «только со снимками» отсеивает почти всё: снимки есть у считаных
-- объявлений. Планировщик шёл по индексу даты, набирая страницу, и перебирал
-- таблицу целиком — запрос по категории со снимками не отвечал.
--
-- Частичный индекс с подзапросом PostgreSQL не допускает, поэтому здесь просто
-- перечень объявлений, у которых снимок есть. По нему база начинает с малого
-- набора и сортирует его, вместо чтения миллиона строк.
CREATE INDEX IF NOT EXISTS idx_listing_images_listing_id
    ON listing_images (listing_id);
