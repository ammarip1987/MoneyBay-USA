{
  // Workspace settings for a Qwen extension in VS Code.
  // Replace the placeholder below with your actual API key if the extension supports this setting.
  // If you prefer not to store the key in the repository, use User Settings or a system environment variable.
  "qwen.apiKey": "YOUR_QWEN_API_KEY_HERE",
  "java.compile.nullAnalysis.mode": "disabled",
  "java.configuration.updateBuildConfiguration": "interactive"
}
